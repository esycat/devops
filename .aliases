OS=$(uname)

alias ll="ls -ahl --color=auto --group-directories-first"
alias psx="ps -ef | grep $1"
alias ports="netstat -altu -np"
alias top="htop"

case $OS in
    Linux)  alias clip="xclip -sel clip" ;;
    Darwin) alias clip="pbcopy" ;;
esac
